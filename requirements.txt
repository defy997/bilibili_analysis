# B站数据分析项目依赖
# Django + MySQL + 数据清洗 + 情感分析

# ============================================
# Django框架及相关
# ============================================
# Django 5.1 是支持 Python 3.10 的最新版本
# 如果要使用 Django 6.0+，需要升级 Python 到 3.12+
Django>=5.1,<6.0
django-cors-headers>=4.0.0

# ============================================
# 数据库驱动 (MySQL)
# ============================================
# 选项1：mysqlclient（推荐，性能更好）
mysqlclient>=2.2.0

# 选项2：PyMySQL（纯Python实现，安装更简单）
# 如果mysqlclient安装失败，可以取消下面的注释使用PyMySQL
# PyMySQL>=1.0.0

# ============================================
# 数据爬取
# ============================================
requests>=2.31.0
beautifulsoup4>=4.12.0
lxml>=4.9.0

# ============================================
# 数据清洗 - 阶段一（文本清洗）
# ============================================
opencc-python-reimplemented>=0.1.7  # 繁简转换

# ============================================
# 数据清洗 - 阶段三（Embedding语义去重）
# ============================================
sentence-transformers>=2.2.0  # 文本向量化
scikit-learn>=1.3.0  # 余弦相似度计算、机器学习

# ============================================
# 深度学习（情感分析 - 仅推理，使用CPU版本）
# ============================================
# 注意：torch 需要单独安装 CPU 版本（更小，约500MB）
# 安装命令：pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cpu
# 如果已经安装了 GPU 版本，可以跳过
# torch>=2.0.0  # 请使用上面的命令单独安装CPU版本
transformers>=4.30.0

# ============================================
# 数据处理和工具
# ============================================
numpy>=1.24.0

# ============================================
# 异步任务队列（Celery + Redis）
# ============================================
celery>=5.3.0
redis>=5.0.0
